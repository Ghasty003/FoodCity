import React from 'react';

interface Props {
    id: number;
    price: string;
    title: string;
    description: string;
    img: string;
}

function Card({id, price, title, description, img}: Props) {
    return (
        <div>
            
        </div>
    );
}

export default Card;